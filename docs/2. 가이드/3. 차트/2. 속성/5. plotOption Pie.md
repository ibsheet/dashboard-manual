# plotOption Pie

## 타입
| 속성 | 타입 | 설명 | 기본값 | 
| -- | -- | -- | -- |
| StartAngle | `number` | 앵글의 시작 점을 설정합니다. | 0|
| EndAngle | `number` | 앵글의 마지막 점을 설정 합니다. | 0 | 

:::note
이 속성은 pie 차트에서만 보입니다.
:::